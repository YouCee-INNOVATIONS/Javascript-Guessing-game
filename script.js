function guessNumber() {
  let randomNum = Math.floor(Math.random() * 11);

  //output to console for debugging
  console.log(randomNum)

  //declare a variable for the guess
  let guess;
  guess = prompt("please enter a number between 0 and 2")
}